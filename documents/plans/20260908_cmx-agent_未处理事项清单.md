# cmx-agent 未处理事项清单

> 日期：2026-09-08 ｜ 模块：`backend/cmx-agent` ｜ 性质：遗留/排期/技术债盘点
> 基线快照：13 crate · **212 测试全绿 · 全仓 clippy 0 告警** · 全部改动**未提交**。
> 关联文档：[Windows 适配方案](20260908_cmx-agent_主流Agent的Windows支持调研与适配方案.md)（P0/P1/P1.5/第二轮已实施）

## 一、等用户动作（阻塞收口的三件事）

| # | 事项 | 说明 | 动作方 |
| --- | --- | --- | --- |
| 1 | **git 提交** | 两轮代码改动（Windows 适配 / 自愈 / pptx / clippy / 文档）+ 用户的 tauri 打包产物（icon.ico、gen/schemas、src-tauri 配置、.gitignore）全部在工作区。建议分两笔：agent 代码一笔、tauri 打包一笔 | 用户说"提交"后执行 |
| 2 | **Tauri 壳重打包** | 已打出的包还是旧 UI；`cargo tauri build` 一次即可吃上：shell 工具卡、🛡 权限下拉（含 localStorage 记忆） | 用户侧 |
| 3 | **端到端验收** | 重启 Web 壳（拿第二轮二进制）→ 新对话实测「做 3 页 PPT」；生成物用 PowerPoint/WPS 打开确认无「修复」提示（pptx 目前只有结构断言，真机打开属人工验收） | 用户侧 |

## 二、已排期、未实施（按里程碑）

| # | 事项 | 排期 | 前置/说明 |
| --- | --- | --- | --- |
| 4 | **P2：Windows 受限令牌 + ACL 沙箱** | M3 | 拍板绑 M3，与 cmx-data-auth 接地同批。**子决策未拍**：网络阻断策略（毒化环境变量 advisory 级够不够）；elevated（专用账户+防火墙）已明确不做。工程量约一周级（unsafe WinAPI + 红队负例） |
| 5 | **macOS/Linux OS 沙箱（Seatbelt/Landlock）** | M3 | bash.rs 头注释 E2 计划项，与 #4 同批做，三平台守卫语义一次收口 |
| 6 | M2 收尾：工具平面接 cmx-* 真实引擎 | 进行中 | connectors 已有 flow/rules/onto/report 骨架，按方案图继续接地真实读写 |
| 7 | M4 多智能体编排 / M5 IM 远程+记忆 / M6 核下沉+GA | 路线图 | 见 AGENTS.md 路线节；每里程碑要求可回放日志+红队用例+真机 e2e |

## 三、技术债与加固（已识别，未排期）

| # | 事项 | 风险/影响 | 建议时机 | 量级 |
| --- | --- | --- | --- | --- |
| 8 | **登录门门户地址写死** `AuthConfig::default() = http://127.0.0.1:8080`（auth.rs:20） | **分发阻断项**：装到企业桌面后连不上门户就登录不进去 | 分发前必改（env/配置文件/model.json 同目录） | 半天 |
| 9 | **Web 壳数据目录在 `%TEMP%\cmx-agent-desktop`** | 系统清理临时目录会丢会话历史与 model.json | 分发前改正式目录（Tauri 壳已是 %APPDATA%，仅 Web 壳） | 2 小时 |
| 10 | UI「工作空间」下拉为占位（index.html:1377 alert） | 沙箱工作区根不可视化管理，用户不知道 agent 在哪个目录干活 | M2 内顺手 | 半天 |
| 11 | 视觉模型未独立配置 | builder.rs 里 vision 走默认（deepseek 系 vision-exp），换 provider 时视觉工具提示「未配置视觉模型」 | 按需 | 2 小时 |
| 12 | **MLamp key 明文**存两份 model.json + 曾在对话中出现 | 泄露面 | 建议轮换一次；长期可接企业密钥管理 | 用户决策 |
| 13 | Win10 兼容未验证（ConPTY/老 PowerShell 5.1 行为） | 企业桌面可能仍是 Win10 | 有真机时过一遍 | 视情 |
| 14 | live 网关测试凭据靠手工 env（CMX_LIVE_API_KEY） | 仅影响开发体验 | 低优 | — |

## 四、外部关联项（不在本仓，但影响本仓）

| # | 事项 | 说明 |
| --- | --- | --- |
| 15 | **根 `AGENTS.md` §1.2 滞后**：仍写"cmx-agent 当前 M1、无 HTTP 端口" | 实际 M2 推进中、13 crate、有 Web 壳（本地 HTTP）。属根仓文件，单独提交 |
| 16 | MLamp 网关偶发掐长流 | 外部因素；客户端自愈已兜（断流重试/采纳），网关侧稳定性归平台团队 |

## 五、明确不做（有记录的"不做"）

- ❌ bash→PowerShell 命令转换器 / 内嵌 POSIX 解释器 / login shell 环境快照 / 强制 Git Bash 依赖（方案 §5.5）
- ❌ Codex elevated 式沙箱（专用账户+防火墙，分发成本高，拍板排除）

---

**建议处理顺序**：#1 提交 → #2/#3 验收 → #8/#9（分发前两个硬项）→ M3 立项时做 #4/#5（含设计文档+红队）→ 其余按表内时机。
