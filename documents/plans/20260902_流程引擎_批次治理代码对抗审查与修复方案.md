# 流程引擎批次治理代码对抗审查与修复方案

> 建档：2026-09-02 ｜ 状态：**v2 已实施完毕（2026-09-02 同日）——§四 4 项用户拍板全采推荐项后按四批全部落地，验证全绿（全仓 75 套件 + http_guard 6 + PG 门控 3/8/3 + E2E 20/20 + clippy 零净新增 + 下游 portalservice check + 资产 diff 零差异）**｜ 审查对象：提交 `c3a635f`（flowengine）+ `6a9fe077`（container）全部批次 0~7 + 001-M1/M2/M3 代码
>
> **实施补录**（与方案的差异，均已回写验证）：①X2-2 闸为纯角色判定（RB-08 采纳——不看 mode，off+无角色自然被拒）；②X3-5 rebuild 确定性 event_id 首版 `rb-{sub}-{iid}-{event_type}` 超长（78 > VARCHAR(64)，E2E 实跑暴露），改为压缩格式 `rb-{sub}-{iid去连字符}-{cmp|trm}`（59 字符）；③X3-6a 续租 `make_interval(secs => $1)` 与 PgInt 绑定类型不兼容（E2E 实跑暴露），改 `($1::int * interval '1 second')`；④三个 PG 门控测试文件均补全局串行锁（并行同库互踩是既有 flaky，顺带修复）；⑤webhook_outbox_pg 两个既有用例的全表回拨 UPDATE 限定 WHERE（共库 dev 环境波及真实业务行）；⑥O-05（前端 catch）在 v2 方案清单中曾被遗漏，实施时补上（真源 todo-center 两处 + publish 同步）。
>
> 关联：`documents/plans/20260903_流程引擎_事件订阅与流程分组方案总档.md`（原 v2.3/v2.4 webhook 方案已合并删除；本方案实施后 webhook 实现形态又被 2026-09-02 事件订阅重构整体替代）、`documents/技术债/20260817_流程引擎_已知问题与技术债务.md`（总账）
>
> 审查方法：四路并行对抗审查（webhook 全链路 / 批次 2 写路径 / 批次 3+4 安全面 / 批次 6+7 治理运维与前端），每项发现均要求代码实证；主审对全部 P0/P1 逐条复验（含 clippy 实跑、SQL 语义核对）。修复方案又经两轮红队交叉评审（A：技术正确性逐项代码验证；B：复杂度惩罚/范围/优先级/部署现实攻击），关键修订：R4-9 由「降级 warn」改有限次重试（RA-01，原设计会静默化父流程永久卡死）、R1-7 由重方案（加列）改轻方案为默认（RB-03/RA-02）、R2-1 闸改纯角色判定并暴露管理页角色落地前置（RB-01/RB-08）、测试从独立批改随批 gate（RB-05，018 纪律）。

---

## 一、审查结论摘要

| 域 | P0 | P1 | P2/P3 | 核心问题 |
| --- | --- | --- | --- | --- |
| W（webhook 链路） | 0 | 5 | 11 | emit fail-open 广播、subscribe 假成功、rebuild 幂等失效、删订阅留孤儿行、legacy 假删除 |
| C（批次 2 写路径） | 0 | 2 | 12 | async 完成路径 CAS 冲突后令牌卡死、rollback 未 await |
| S（批次 3+4 安全面） | 0 | 7 | 16 | 草稿写闸校验对象错误、endpoints 死配置、webhook 写闸无角色、列表双通道未收口、system 隔离不挡详情、写闸漏挂绑定、零测试 |
| O（批次 6+7 治理运维） | 1 | 5 | 12 | incident 自动重试 SQL 必错且静默、retention 漏 2 表、demo 假移出、cancel 不清 incident、前端无 catch、零测试 |

**结构性观察**：①「声明与实现不符」贯穿四域（注释/提交说明宣称的能力在代码里不存在）——最优先清账；②收口类改动呈「逐端点手挂」模式，漏挂数大于覆盖数——本批补齐高价值漏挂，路由层中间件统一闸另立债项；③两个「新增功能零测试」直接导致 O-01 必炸 SQL 上线——**测试随修复批落地（018 gate），不设独立测试批**。

**前置声明（RB-02，诚实边界）**：本批全部安全修复约束的是「无角色 JWT 用户」面。当前 5 份 toml 仍含真实凭据且被 git 跟踪、E2E/AGENTS 使用同一把 allow-all legacy key——**key 持有者（service 角色）在设计上全放行**，对 threat model 里真实攻击者的收敛依赖 019（key 换值轮换 + 结构化拆分），属用户环境操作，本方案只在发布说明中强提示。

---

## 二、修复内容（按四个提交切分）

### 提交一 · 删除清账 + 正确性修复（无行为变更，E2E 必绿）

先删后改（RB-04）：R3-1/R3-2 的删除区与 R1-6/R4-15 的改造区同片，先清账避免绕着死代码做签名改造。

| # | 来源 | 问题 | 修复设计 | 触点（全路径） |
| --- | --- | --- | --- | --- |
| X1-1 | W-05 | M3「删 legacy」是假删除：~170 行内存链路死代码、恒 disabled 的 `FlowRuntime.webhook` 字段、零调用的 `WebhookMode/webhook_mode()`、自等断言测试、仍在跑的 legacy 集成用例、死配置 `FLOW_WEBHOOK_MAX_RETRIES` | 真删上述全部；**保留**三头常量 + `sign_body` + `WebhookTarget`（`channel_webhook.rs:11` 复用中）；legacy 集成测试中仍有效的契约断言迁移至 channel_webhook 测试，纯 legacy 用例删除 | adapters/webhook.rs、app/engine.rs、webhook_outbox.rs、adapters/tests/webhook.rs |
| X1-2 | O-03 | demo 被移出 `members`（注释却说「保留可寻址」）→ 目录成不可构建死代码；`default-members` 与 `members` 逐项相同 = 冗余 | demo 加回 `members`（12 项）、`default-members` 维持 11 项不含 demo——注释即成立；顺带清 demo `web/index.html` 的 riskLevel 残留（O-15） | flowengine Cargo.toml |
| X1-3 | O-01 | incident 自动重试扫描 SQL `SELECT DISTINCT ... ORDER BY updated_at` PG 必报错 + `let Ok else continue` 吞错，功能 100% 失效零信号 | 改 `SELECT instance_id FROM cmx_flow_incident WHERE state='OPEN' GROUP BY instance_id ORDER BY max(updated_at) LIMIT 20`（ASC 保持最旧优先；`idx(state,updated_at)` 过滤支撑，OPEN 行量小聚合可接受——RA-05）；失败分支补 `tracing::warn!` | **cmx-flow-app/src/engine.rs** `spawn_incident_retry` |
| X1-4 | C-02 | `exec_save_ops` CAS 段 map_err 闭包内 rollback 未 await（同步闭包放不下 await），SQL 错误时事务悬挂 | CAS 段改 `match` 结构，错误分支与 0 行分支同样 `rollback(&txn_id).await`（对齐函数内既有正确形态） | store-pg store.rs:75-81 |
| X1-5 | C-01+RA-04 | `complete_async_job` 先删作业行（独立事务）后 CAS save；冲突时作业已删、delegate 已执行、令牌永卡 WaitingAsync 无自愈 | store 层 DELETE 改 `RETURNING` 全列（async_job 12 列可行）；引擎持作业数据，save `Err(Conflict)` 分支**补偿重插**（`ON CONFLICT (id) DO NOTHING`，`locked_by/lock_expires_at` 原样保留——租约 ≤60s 过期后可重抢）后原样上抛。**行为声明**：该作业在锁过期后被重抢、delegate 至少一次重执行（at-least-once，与 `trigger_due_timers` 结果对齐）；带 topic 的外部作业依赖外部 worker 再次轮询，409 响应文案注明可重试。**触点三处同改**：trait 签名（model/store.rs）+ PG 实现 + memory_store 实现（RA-04：漏 trait 直接编译失败） | model/store.rs、store-pg/store.rs、engine/memory_store.rs、engine/engine.rs |
| X1-6 | O-04+RA-03 | cancel/withdraw 终态化不 resolve incident 台账 → 幽灵 OPEN 永久残留（auto-retry 默认关闭时无任何收敛路径）；`complete_async_job` 重试成功路径同样不关账 | ① `cancel_process`/`withdraw_process` 终态化 **save 成功后**调 `resolve_incidents_by_instance`（失败仅 warn）；② `retry_incident` 的 `reactivated==0` 提前返回分支补 resolve；③ `complete_async_job`（含 `retry_dead_letter_job` 成功分支）推进成功后补 resolve——异步作业恢复的三条路径全部闭环（RA-03） | cmx-flow-engine/src/engine.rs |
| X1-7 | O-02 | retention 十表清单漏 `cmx_flow_incident`（有 instance_id 列+索引）、`cmx_flow_deadletter_job`（有 instance_id 列+索引）——实例删后幽灵行无限累积（死信行重投自愈依赖人工触发） | 表清单补两表，随实例按同一 30 天口径删 | cmx-flow-app/src/engine.rs `purge_terminal_instances` |
| X1-8 | S-01 | 草稿写闸校验 `req.name`，实际 key 由 BPMN `<process id>` 派生（`save_draft_checked` 第一行同 `validate_bpmn`）——传合规 name + BPMN 写他人命名空间 key 即绕过 | handler 先 `cmx_flow_def::validate_bpmn(&req.bpmn_xml)` 解真实 key 挂闸（与内部派生**同源同值**，RA 验证），name 仅展示；双编译毫秒级可接受 | handlers.rs save_definition_draft |
| X1-9 | O-16/O-09 | clippy 两处净新增 `&&str` 警告（engine.rs:2632/3110）；`outbox = true` 恒真死变量 | 去 `&`；删变量简化条件 | cmx-flow-engine/engine.rs、cmx-flow-app/engine.rs |
| X1-T | 随批测试（gate） | — | `incident_lifecycle_pg.rs` 新建（#[ignore] 门控）：incident 落表→retry→RESOLVED；cancel 后 resolve；purge 后 **12 表**归零 + hi 保留；retry scan SQL 可执行 | cmx-flow-app/tests/ |

### 提交二 · 行为变更：fail-close 与安全收口（先拍板 §四，后动工）

| # | 来源 | 问题 | 修复设计 |
| --- | --- | --- | --- |
| X2-1 | W-01 | handler 层 4 处 emit 借快照失败折叠为「未绑定」→ fail-open 广播给全部通配订阅（违背 v2.4 R3 fail-close） | emit 传参改三态 `SubscriberRoute::{Bound(i64), Unbound, Unknown}`；`Unknown` 复用 `emit_to_outbox` 既有 Some(sid) 失败支路（只投显式旁听 listen_matches + `EMIT_LOOKUP_ERRORS` 计数）；SSE 半边不动（先于 emit 且独立）。**被排除的更轻选项记录（RB-09）**：快照失败时完全不投 webhook（只 SSE+计数）同样 fail-close 且改动更小——不取，因旁听订阅的显式声明值得在异常窗口保住（显式订阅是接收方主动意愿，静默全丢比少投更伤信任）。Unknown 且缓存也失败时补 warn（零投递与无订阅可区分，RA-10） | 
| X2-2 | S-03+W-13+RB-01/RB-08 | webhook 13 个管理写端点无角色门槛；`WEBHOOK_ADMIN_ROLE` 零消费；off 形态经「中间件恒 active」绕过 | 闸改**纯角色判定不看 mode**（off+key 合法形态不受影响，RB-08）：`roles 含 service`（M2M key）或 `flow-webhook-admin` 或 `flow-admin` → 放行；否则 403（`FlowError::forbidden`，修 W-13 的 200+code=1）+ operator 审计日志。**管理页角色落地前置（RB-01，§四-2 拍板）**：门户反代剥 key 保留 JWT，浏览器链路解出的用户当前无占位角色——直接上线即断管理页写操作 |
| X2-3 | S-04+S-08 | 实例列表双通道未收口：`GET /instances` 全量吐 100 条摘要；`POST /instances/query` initiator 传谁查谁 | ① list_instances 补 system 过滤（`current_system()` 有值时）；② `effective_query_user` 的 admin 分支改「显式传 qu 放行 qu」（真代查），非 admin 传他人维持 403。**行为变化面声明（RA-07）**：该函数 5 个调用点（待办/我发起/抄送我/我已办/query_instances）的 admin 代查从「假代查查自己」变「真代查」，E2E 走 X-API-Key（user=None 原值分支）不受影响 |
| X2-4 | S-06 | 006 写闸漏挂子流程绑定（upsert 可改任意绑定 target，影响运行路由） | upsert 对 `called_key` 与 `target_key` 双资源挂 `ensure_write_access`；delete 按 id 先解析回 called_key 再挂闸 |
| X2-5 | S-02 | `KeyDecl.endpoints` 端点类别白名单零消费方——虚假安全感 | **拍板删除**（RB-07：非选择题——无消费方的机制按复杂度惩罚砍掉；serde 无 deny_unknown_fields，存量配置静默兼容已验证）：删字段 + `endpoint_allowed()` + config.rs:113 注释 + CONFIG_MANUAL 同步；端点级限权若有诉求随 003 二期按路由前缀实现 | 
| X2-6 | S-05 | 005 system 隔离只挡列表不挡详情：结构化 key 按 id 可读任意系统实例全量变量 | `ensure_instance_participant` 对 `current_system().is_some()` 的调用方追加实例 `system_id` 匹配（参数化 SQL；实例 system_id 为 NULL 的存量行放行——两阶段口径）。**§四-3 拍板**（收紧 M2M 读取面，可能影响平台侧 key 跨系统运维查询） |
| X2-7 | S-10/O-17 | 干预端点（complete_async_job/fail_async_job/correlate 可 merge 任意变量；migrate/jump/suspend/resume/retry-incident/urge/dead-letter 无角色无 delegation） | 变量操纵类（async-jobs complete/fail、correlate、set-variables）挂 `ensure_delegation_valid` + system 归属；干预类（migrate/jump/suspend/resume/retry-incident/urge/dead-letter retry·discard）挂 `flow-admin` 角色或 delegation。**§四-4 拍板**（本批做 vs 另立批次——10+ 端点行为变更） |
| X2-T | 随批测试（gate） | — | `http_guard_test.rs` 新建（两层：`tenant::scope` 注入身份的直接断言 + 一条真中间件冒烟——RB-10 估 250~350 行）：草稿闸按 BPMN key 拦截、webhook 闸三态（service/角色/无角色 403）、query_instances initiator 校验、reject 无身份拒绝。**前提（RA-09）**：闸判定不依赖 `AUTH_ACTIVE`（纯角色判定天然满足） |

### 提交三 · 收敛与参数化（R3 余项 + R4 合批）

| # | 来源 | 修复 |
| --- | --- | --- |
| X3-1 | W-04（RB-03 轻方案） | retention 自动清理路径（`purge_deliveries` 系统任务，不需租户语义）**去 subscription 子查询**，按 `state + created_at` 全局清——消除「删订阅后行永远清不掉」的 P1 本体；管理查询侧孤儿行不可见降级为 P3 文档声明（subscription_name 快照仍在行内）。tenant_id 列重方案（含 RA-02 的 JOIN 回填）**砍单待用户翻案**（§四-1）：多接收方/多租户管理面诉求出现时再做 |
| X3-2 | W-14 | save 端点合并双查为一次；`definition_keys` 复用 `norm_def_keys` 归一化 + 单键 ≤128 校验 |
| X3-3 | W-06 | 拆 `get_subscription_for_route`（无 COUNT）；`emit_to_outbox` 批外点查一次复用 |
| X3-4 | W-02 | subscribe/unsubscribe UPDATE 返回行数；0 行时重查分类返回 400（消并发窗口静默假成功） |
| X3-5 | W-03 | rebuild event_id 改确定性 `rb-{subscription_id}-{instance_id}-{event_type}`（uk 幂等真正生效）；超 LIMIT 2000 响应注明截断 |
| X3-6 | W-07/W-08/W-09 | 续租改 `now()+interval`（DB 时钟）；mask_secret 加 char_boundary；finish_done 清 locked_by |
| X3-7 | C-03（RB-06 定位：测试保真） | InMemoryStore `save_snapshot` 补 version CAS（对**存储副本** +1，不回写调用方）；排批末不阻塞验收 |
| X3-8 | C-06/C-07/C-09 | trigger_due_timers 冲突分支按 instance_id 剔除 fired；fail_async_job 耗尽冲突 warn 升 error；trait 契约补「须以 load 所得 version 做 CAS，冲突返回 Conflict」+ 修正三处陈旧注释 |
| X3-9 | RA-01（原 R4-9 重设计） | `complete_subflow` 父实例推进冲突改**有限次重试 2~3 次**（重 load 父快照重放，天然幂等；短退避），耗尽 error + 计数——**禁止降级 warn**（「自然补齐」论据被红队证伪：子实例终态后无任何再触发事件源，warn 吞掉 = 父流程永久卡死零信号） |
| X3-10 | O-08（RA-08 数字修正） | incident 主键改 `Uuid::new_v4()`（现状 `inc-{36字符}-{node}` 拼串 node>19 字符即超 VARCHAR(64) 静默丢失；幂等由 uk(instance_id,node) 承担） |
| X3-11 | S-11/S-12 | reject 对齐 complete（auth active 无身份拒绝 + from_user 兜底校验）；withdrawable 走 effective_query_user；mark_cc_read 补 `AND to_user_id`；get_biz_instances 挂参与方（None→放行口径维持，mdm 链路不断） |
| X3-12 | S-16+W-13 余项 | webhook_store.rs 17 处 `esc()` 拼接改 SqlCond 参数化（与 X3-4/X3-6 同函数一次改净，避免 RB-03 指出的两轮返工）；ensure_auth_gate 家族其余 business_error 拒绝改 forbidden |
| X3-13 | O-07/O-12/W-11/S-14（RB-11 拍死进本批） | HELP 措辞如实缩口径 + `text/plain; version=0.0.4` + CONFIG_MANUAL 补 /metrics 抓取鉴权说明；skip 端点放开 `PENDING→SKIPPED`（停用订阅存量行的人工弃投出口）；key+Authorization 并存时 warn |
| X3-T | 随批测试（gate） | `cas_conflict_pg.rs` 扩展（cc 已读并发不回滚 / 冲突子表不变 / 台账 DO NOTHING 幂等）；`webhook_outbox_pg.rs` 扩展（subscribe 0 行 / rebuild 确定性 event_id / retention 清孤儿行） |

### 提交四 · 文档回写（D 档）

| 来源 | 处置 |
| --- | --- |
| W-15 | mon 独立端点（业务指标与技术监控分立）回写 v2.4 §3.6 措辞 |
| W-11 | 停用订阅存量 PENDING 行继续投递——docs/usage/08 + 管理页停用文案明示（skip 放开已随 X3-13） |
| C-04 | 008 租约可被并发 save 重插抹除——注释降级「advisory 租约、CAS 收敛」如实描述 |
| C-10 | 旁路追加仍走全量 CAS（notify_cc 并发 409）——总账 007 补「追加型旁路写未独立化」残余 |
| S-13 | start_by_message 的 system_id 断点——函数注释标注接线义务 |
| S-14 | key 优先级文档化（warn 已随 X3-13） |
| S-21/22/23、O-14 | orgs/identity 收敛、拒绝路径审计、常量时间、画布 25 处硬编码——总账补录残余 |
| RB-02 | 发布说明强提示：019 key 轮换未执行前，本批安全修复不约束 key 持有者 |
| 总账销账 | 本轮修复项逐条销账 + 结构性观察（逐端点手挂模式）记入 004 演进方向 |

**明确不做**（复杂度惩罚/范围裁定）：路由层中间件式统一鉴权分组（中型改造，总账另立债项）；`business_errors` 计数下沉信封层（013 范围）；purge 批量化 ANY($1)（量级未到）；incident 台账挪 save 后（接受单向丢失声明 + 冲突 warn）；S-18 identity local 提权链（单机形态随 identity 模块收敛）；S-15 api_keys 解析失败 panic 化（改启动行为影响 8 下游仓，随 019 配置治理一并，本批 error 日志维持）；delivery 表 tenant_id 列（轻方案已覆盖 P1 本体，重方案待 §四-1 翻案）。

---

## 三、实施与验收

```
提交一（清账+正确性，无行为变更）→ 用户拍板 §四 → 提交二（行为变更+闸）→ 提交三（收敛）→ 提交四（文档）
```

每提交独立 `cargo check + clippy（零净新增）+ cargo test`；提交二后 E2E 复跑（`/tmp/flow_e2e3.sh`，X-API-Key 通道设计上不受影响）+ 新闸用例；提交三后 PG 门控测试本地全跑；container 侧改动（X2-5 删字段）后 flowengine + portalservice `cargo check`（engine-kit 仅字段删除，API 面收缩）。前端资产（X3-13 的管理页文案若涉）走真源 + `publish-assets.sh flow`。

**验收口径**：① 全部 P0/P1 有修复或用户拍板处置；② clippy 相对 c3a635f 零净新增且消掉既有 2 处；③ 随批测试全绿（PG 门控组本地 PG 实跑）；④ E2E 20/20 维持 + 新增闸用例通过；⑤ 总账同步销账 + D 档登记。

---

## 四、待用户确认项（v2 修订后 4 项）

1. **X3-1 delivery 表 tenant_id 列**：轻方案（本方案默认——retention 去子查询 + 孤儿行不可见降 P3 文档）已消「无限增长」P1 本体；重方案（加列 + JOIN 回填 + 管理查询直查，约 150~250 行 + 唯一 DDL 迁移）仅在需要「删订阅后流水仍按租户可查」的审计面时才有净收益。**推荐：轻方案，重方案砍单待诉求**。
2. **X2-2 角色落地前置（RB-01）**：闸上线即断管理页浏览器写操作（门户 JWT 用户无占位角色）。三选一：(a) 预置 `flow-webhook-admin` 平台角色并授予管理员（最正，需平台角色侧配合）；(b) 过渡期把平台 `admin` 角色并入豁免集（最平滑，推荐）；(c) 仅 key 可写，管理页写操作暂不可用（最紧）。
3. **X2-6 详情面 system 收紧**：结构化 key 按 id 读实例时校验 system_id 匹配（005 闭环，推荐）vs 维持「系统边界由 003 声明治理」过渡态（平台侧 key 跨系统运维查询不受影响）。
4. **X2-7 干预端点收口范围**：本批收紧（变量操纵类挂 delegation+system，干预类挂 flow-admin——10+ 端点行为变更，E2E 脚本相关用例需适配）vs 另立批次（本批只修审查发现的结构问题，越权面登记总账 004 残余）。
