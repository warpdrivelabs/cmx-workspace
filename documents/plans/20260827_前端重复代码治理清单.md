# 前端重复代码治理清单

> 来源：2026-08-27 全量实测审查（463 个源文件哈希扫描 + 逐处 diff 验证）。
> 核心结论：**零字节级重复文件**；问题集中在「同义重写 + 分叉克隆」——同一件事各写一遍、各自微调、各自修 bug，行为已分叉。
> 负责人列留白，由 @负责人 分配后回填。

规模基线（排除 node_modules/dist/coverage/vendor）：

| 位置 | 文件数 | 行数 |
| --- | --- | --- |
| `packages/cmx-data-comp/src` | 175 | 60,699 |
| `cmx-container/assets`（页面资产真源） | 169 | 48,073 |
| `../../cmx-portal-manager` | 132 | 39,772 |
| `../../cmx-html-designer` | 116 | 26,873 |

---

## P0 — 已造成缺陷 / 隐患正在扩大

| 编号 | 事项 | 位置与证据 | 建议做法 | 验收标准 | 工作量 | 负责人 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A-01 | 属性注入缺陷：`esc` 不转双引号却用于属性值 | `cmx-container/assets/mdm/web/ui-native/portal/mdm/master-detail.js`（`esc` 只转 `&<>`；`label="${esc(l)}"`、`name="${esc(st.icon)}"` 两处属性内插值）。对照：同目录 `master-list.js` 是正确的 `esc`+`escAttr` 一对 | 补 `escAttr`（或把 `esc` 升级为转 `"`），属性插值处改用它 | 列 caption / icon 含 `"` 时详情页不破坏 DOM；mdm 其他页面回归正常 | 0.5h | Qoder | ☑ 2026-08-27 |
| A-02 | `api-client.js` 分叉克隆：Designer 缺 Portal 已修的 3 个 bug | `../../cmx-portal-manager`(258行) vs `../../cmx-html-designer`(244行)，相似度 94%。Designer 缺：①登录页判定 `endsWith('/login.html')`（Designer 用精确 `===`）；②回跳只取干净 pathname 防 redirect 参数指数膨胀（Portal 注释明确这是 HTTP 414 根因）；③业务错误透传 `data.violations` | 收敛为一份共享实现（落点建议 `packages/cmx-ui5-runtime` 或新 `packages/cmx-app-kit`），登录页路径等差异项参数注入；两端 import 同一份 | 未登录访问 `/portal/`、`/html/?id=...` 跳转正常且不回环；列校验 `violations` 明细在 Designer 侧可拿到；两端 `npm run build` 通过 | 1d | Qoder | ☑ 2026-08-27 |

> **A-01 完成记录**（2026-08-27）：按 `master-list.js` 范式补 `escAttr`（转 `&<>"`），两处属性插值（`label=`/`name=`）改用之，文本插值保持 `esc` 不变。验证：`node --check` 通过；属性上下文 `esc(` 残留 0 处。已通过 `publish-assets.sh mdm` 同步发布产物 `cmx-mdm/web/ui-native/`（同步前产物与真源仅此一个文件不一致，同步后哈希一致）。注：Windows 下直跑该 sh 因 CRLF 报 `set: pipefail` 错，需转 LF 临时副本执行。
>
> **A-02 完成记录**（2026-08-27）：共享实现落在 `packages/cmx-ui5-runtime/src/api-client.js`（以 Portal 修复版为准；文件本就由 `import.meta.env.BASE_URL` 驱动、应用无感），`package.json` 加 `./api-client` exports，两端 `vite.config.js` 按惯例加逐一映射 alias。10 处 import（Portal 8 / Designer 2）改为 `cmx-ui5-runtime/api-client`，两端旧文件删除。验证：node 解析 9 个导出正常；Portal/Designer 构建通过，产物确认各自注入 `/portal/login.html` / `/html/login.html` 且 3 个修复均在两端产物中；Designer 测试 111/111 通过；改动文件 lint 零告警。**附带修复**：`vitest.config.js` 补 `cmx-ui5-runtime` alias——`normalize-server-page-html-for-debug.test.js` 的 exports 拦截失败为存量问题（git stash 基线复现），与本次改动无关，现已一并修复。注：`npm install` 因私服禁 remote 拉取（EALLOWREMOTE，saxen 包）失败，与本次改动无关（依赖图未变，workspace junction 直读 package.json）。**待人工**：未登录访问 `/portal/`、`/html/?id=...` 的跳转回环手测（需起服务）。

## P1 — 收敛基建与存量清理（先基建后清理，避免边清边长）

| 编号 | 事项 | 位置与证据 | 建议做法 | 验收标准 | 工作量 | 负责人 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| B-01 | 原生页面共享 helper 出口 | 页面资产 37 个文件各自定义 `esc`（3 种语义）、17 个各写 fetch 封装、18 个各写 toast。机制已存在：`cmx-data-comp` barrel 经 Portal `import-ui5-and-app.js` 挂到 `globalThis.__cmxDataComp`，`init-page-models.js` 已按此模式导出辅助函数 | 在 `cmx-data-comp` 新增 `lib/cmx-page-helpers.js`（`escHtml`/`escAttr`/`apiJson`，语义取最严格版：转 `&<>"'`），barrel 导出 + 文档进 `cmx-components-guide` | 原生页面 `const { escHtml } = globalThis.__cmxDataComp` 可用；`npm test -w cmx-data-comp` 通过 | 0.5d | Qoder | ☑ 2026-09-01 |
| B-02 | `__cmxDataComp` 全局契约双形态统一 | Portal：整 barrel（`import-ui5-and-app.js`）；Designer：`src/plugins/cmx-models-plugin.js` 手工挑 11 个成员——同名全局两套能力边界，且 barrel 每次新增都要手工同步 | Designer 侧改为整 barrel（或最小收敛：导出契约集中到一个文件，两端共同 import） | 同一原生页面在门户运行与设计器预览取到的 `__cmxDataComp` 成员一致 | 0.5d | Qoder | ☑ 2026-09-01 |
| B-03 | 37 份页面 `esc` 收敛 | 见 B-01 证据；含语义不一致（2 份不转 `"`、16 份不转 `'`、19 份全转） | 依赖 B-01：逐页替换为共享版；不转 `"` 的 2 份（`mdm/master-detail.js`、`mdm/cr-form.js`）替换前逐处检查插值上下文 | 资产目录内 `function esc|const esc` 存量清零（grep 验证）；相关页面手测通过 | 1d | Qoder | ☑ 2026-09-01 |
| B-04 | flow 8 页 `apiJson`（5 个变体）+ mdm 5+ 页 `apiGet` 收敛 | `flow/web/ui-native/flow/` 下 8 页同名同签名、去空白指纹分 5 组（4 份一致 + 4 份各不同）；mdm 侧 `apiGet(url, dbId)` 同型 | 依赖 B-01：语义对齐到最完整变体（信封解包 + HTTP 错误 + msg 透传）后逐页替换 | 各页 CRUD/查询手测通过；错误提示文案不回归 | 1d | Qoder | ☑ 2026-09-01 |
| B-05 | 18 页自造 toast → 共享 `showCmxToast` | `cmx-data-comp` barrel 已导出 `showCmxToast/showCmxError`，实测页面采用率 **0/18**（flow 8、mdm 4、portal 2、report 4） | 零基建：逐页删除本地 toast，改调共享版；顺手把"发现共享能力"写进页面技能 | grep 自造 toast 定义清零；各页提示交互不回归 | 0.5d | Qoder | ☑ 2026-09-01 |

> **B-01 完成记录**（2026-09-01）：新增 `packages/cmx-data-comp/src/lib/cmx-page-helpers.js`——`escHtml`/`escAttr` 同实现（`&<>"'` 五字符最严格集合，双名只表达文本/属性上下文意图）；`apiJson(url, options, cfg)` 取全变体并集语义：信封解包（无 data 字段原样返回）+ `msg||error` 双字段名透传 + `业务错误 N` 回退 + 错误对象挂 `.status/.body`，并把 report 变体的结构化错误契约（`.code/.conflict(409)/.violations/.validation`）收编为共享能力；`apiGet(url,dbId)/apiPost(url,payload,dbId)` 便捷版签名对齐 mdm 存量。cfg 三键（apiBase/fetchInit/authHeaders）保留 flow 页组件壳 configure () 通路。barrel 已导出（index.js）；新增 11 个单测全过。文档：技能 `cmx-components-guide` 新增 `references/page-helpers.md`（含与应用侧 escape.js 的语义对照表）+ SKILL.md 索引；顺带修正 `frontend-conventions.md` 里 api-client.js 的滞后路径（A-02 已迁 `cmx-ui5-runtime`，文档未跟上）与 `common-mistakes.md` 陷阱 3 的 barrel 现状备注。
>
> **B-02 完成记录**（2026-09-01）：`../../cmx-html-designer` 手挑 11 成员的 `__cmxDataComp` 改为 `import * as cmxDataComp from 'cmx-data-comp'` 整 barrel 挂载，与 Portal 同契约；边际体积可忽略（Designer 的 cmx-data-plugin 本就全量副作用引入了全部可视组件）。`node --check` 通过，需用户 build Designer 验证。 **附带修复**：Portal `import-ui5-and-app.js` 的 barrel 挂载原为 `.then()` 不阻塞——深链首开 tab 时原生页面模块顶层解构 `globalThis.__cmxDataComp` 存在竞态，已改为 await 链（挂载完成后才继续主题初始化/组件注册），这是 B-03/B-04/B-05 页面顶层解构的安全前提。
>
> **B-03 完成记录**（2026-09-01）：实测 **39 个文件**（清单 37 的口径差异：实测含 model 2 / onto 3 / report 5，另 vendor `cmx-ontology-graph.js` 属第三方不动）。批量脚本删本地定义 → 原位插入 `const { escHtml: esc } = globalThis.__cmxDataComp`（有 escAttr/escHtml 的按名并入），调用点零改动（别名解构）。两处点名文件核过上下文：`cr-form.js` 11 处调用全为文本节点插值（另发现并清除其函数内第二份遮蔽定义）；`master-detail.js` 属性上下文 A-01 已补 escAttr。严格集合是旧语义超集，替换只多转不少转。验证：资产目录 `function esc|const esc|escAttr|escHtml` 本地定义 grep 清零（vendor 除外）；39 文件 `node --check` 全过。
>
> **B-04 完成记录**（2026-09-01）： **43 个文件**收敛（清单点名的 flow 8 + mdm 10，扩至同型全部：onto 3 / rules 6 / report 5 / model 5 / portal 6）。两形态：① CFG 页（flow 8/onto 3/rules 6）保留本地薄包装 `apiJson(url,options) → 共享版(…, CFG)`，configure () 组件壳契约不丢，flow design-workbench 的 X-User 头在包装内显式合并（人工处理）；② 直连页直接解构 `const { apiGet, apiPost } = globalThis.__cmxDataComp`。mdm/model 的本地 `unwrap` 仅在无其它调用者时一并删除（`model/dct/data-editor.js` 有 1 处独立调用者故保留）。行为等价性依据：门户 fetch 拦截器透明拆信封且错误体保 `msg/error` 双名，新旧封装在拦截器之上行为一致。 **未动**：fi 3 个凭证页（`voucher-native*.js`，固定 DB_ID 闭包形态，不在清单范围，留待后续批次）。验证：43 文件 `node --check` 全过。
>
> **B-05 完成记录**（2026-09-01）：18 文件四族签名全部映射到 `showCmxToast`：flow `toast(msg)`→别名解构零调用改动；`toast(msg,err)`/`toast(st,msg)`→丢参/映射 error 级；mdm+dam `showToast(msg,tone,duration)`→`{level:MAP(tone),duration}`（ok→success/warn→warning）；registry-editor `toast(kind,text)` 参数逆序映射；report `toast([root,]msg,kind)`。5 个孤儿 `_toastTimer` 声明清除；旧 toast 的 CSS/占位元素残留无害保留（最小 diff）。验证：本地 toast 定义 grep 清零；全部资产 `node --check` 通过。
>
> **P1 收尾**（2026-09-01）：`npm test -w cmx-data-comp` 760 passed / 2 failed——2 个失败为存量基线（cmx-column toBe 引用相等、DCT 系统列排序，与本次无关）；改动文件 ESLint 零告警。资产发布：`publish-assets.sh` 跑 portal/model/mdm/flow/report/rules 六域（Windows CRLF 坑用仓内 LF 临时副本法），抽验 6 文件哈希与真源一致；onto 3 页只改真源（无本地 publish 目标，远端 cmx-onto-server 部署自带）；`check-asset-ownership.py` 顺手补 onto 前缀映射修掉存量 KeyError。 **待人工**：① build Portal + Designer（cmx-data-comp 源码变更，native-page 生效依赖 Portal 重建）；② 各页 CRUD/查询/提示交互手测；③ fi 3 凭证页后续批次。
>
> **C-01 完成记录**（2026-09-01）：新建 `../../cmx-html-designer` 单一出口，转引 `cmx-data-comp/lib/cmx-page-helpers.js`（`escHtml as esc` + `escAttr` + `escHtml`，最严格五字符集合）； **8 个文件**（清单 7 + 同目录 `models-event-script-hints.js` 顺带）删除内联定义改 import，调用点零改动（超集语义，masterslave 旧版不转 `>` 也被覆盖）。验证：目录内 esc 定义 grep 归零（仅剩 utils/esc.js 一处出口）；9 文件 `node --check` 通过；`npm test -w cmx-html-designer` **111/111 全过**（好于存量基线——A-02 补的 vitest alias 使此前加载失败的文件恢复）。需用户 build Designer 验证。
>
> **C-02 完成记录**（2026-09-01）：5 个 models-props 徽章去内联 `style="#XXX20/#XXX"` 改 `data-kind`；配色下沉 `designer-page-data-shell-style.js` 的 `.props-type-badge[data-kind=...]`：dataset→`--sapInformativeElementColor`、columnmodel→`--sapPositiveElementColor`、meta→蓝绿两令牌 `color-mix` 各半、flc→`--neo-violet`（neo 副色令牌，无则兜底原色）、masterslave→`--sapCriticalElementColor`；底色一律 `color-mix(... 12%, transparent)`（对齐原 `#XXX20` 视觉）。var () 内兜底原色属合规（§四.5 口径）。顺带修正原文件两处笔误色（columnmodel `059869/059669`、masterslave `b45909/b45309` 不一致）。验证：面板目录 `style="background:#` 归零。 **待人工**：暗色主题下徽章可读性目测。另发现同面板 `page-data-panel-models.js` L180 `model-type-label` 内联 `color:${info.color}`（MODEL_TYPES 色表）为同类违规，归入 C-03 批次。
>
> **C-03 报表**（2026-09-01）：分类报表已归档 `documents/plans/20260901_前端_裸色值分类报表.md`——四分类（var 兜底合规 3440 / neo 令牌定义 68 / 图表色疑似 7 / **真违规 1110 处·144 文件**；口径含排除 vendor/tests/demo，与清单估算差异源于此）。最大单点：`../../cmx-portal-manager`（126 处）。批处理未启动。
>
> **C-03 报表 v2（口径修正）**（2026-09-01）：v1 把 CSS 自定义属性 **定义行**（`--x: #yyy`，主题文件本职）误计入违规。v2 重新归类后： **真违规 926 处 / 141 文件**（令牌定义 252 行转合规）。最新最大单点：`portal/datasource/cluster-datasource.js`（118 处，多为主题 hover/选中态裸值，语义可映射 sap 令牌）、`rpt/designer.js`（77 处）。行级清单即施工图；批处理须逐处语义判断 + 亮暗双主题目测（§四.5），未在本轮启动。
>
> **C-04 完成记录**（2026-09-01）：实测两端 diff 仅 **10 行品牌文案差异**（title/brand/feature 副标题），并非清单所记"仅 base 路径差异"——抽模板需参数化 5 处文案位，性价比低，故采用文档认可的备选方案：两端文件头加同步提醒注释。注释方案无行为变化，无需测试。
>
> **C-05 完成记录**（2026-09-01）：新增 `packages/cmx-data-comp/src/lib/cmx-deep-clone.js`（`deepClone`：null 透传 + structuredClone 优先 + JSON 兜底，取原 cloneJson 最完整语义；`deepCloneArray`：非数组归一 []）。四处副本回接：cmx-meta-model `cloneJson` / flc-overlay `clone` / cmx-field-clipboard `cloneArray`（import 别名，调用点零改动）+ Portal `portal-definition-manager.js` `clonePlain`（15 个调用点经别名保持不动，子路径 import）。barrel 导出。新增 7 个单测（含 structuredClone 缺席回退路径）全过。验证：data-comp 767 passed / 2 存量基线失败（同前）；改动文件 `node --check` 通过。需用户 build Portal + Designer。
>
> **fi 3 凭证页收敛**（2026-09-01，P1 B-04 补遗）：`voucher-native.js` / `voucher-native-zmcjson.js` / `voucher-native-bin.js` 删本地 `unwrap`/`apiGet`/`apiPost`，改共享 `apiJson` 薄包装（保留 db_id: 'fico-db' 头与"为什么不用 loadDocData"注释）。行为等价性已实测核验：sqlx-dataset-json 返回原始信封 `{code,msg,data:{datasetId,columns,rows,childRows}}`，pkg 无 `data` 键 → 共装 apiJson 的 `'data' in j` 启发式在拦截器拆包/未拆包两模式下均安全。 **浏览器实测**：门户 5173 打开「会计凭证② (native·DataSet)」点加载 → 「已装载 凭证 50 行」，四层网格（批/分录/辅助/维度）数据齐全。bin 页的 save 路径未实测（避免写库），代码与已验证的 apiGet 包装同构。
>
> **全维度审查**（2026-09-01）：在重复代码专项之外做了 14 维度全量审查（安全/错误处理/泄漏/lint/色值补扫），发现 lint 门禁失守、html-pages esc 属性插值（A-01 同款）、裸空 catch 393 处等新项，见 [20260901_前端_代码完整审查报告](20260901_前端_代码完整审查报告.md)。
>
> **B 级收敛第二轮**（2026-09-01）：审查报告 B 系四项全部落地——B3 esc 残留 12 文件收敛（含 Portal escape.js 整体转引）、B4 深拷贝 38 处 → 共享 deepClone、B2 双端 10 个 api 文件 readData/readErrorMessage/fetchJson → ui5-runtime apiFetch、B1 html-pages 弱 esc 全量五字符化 + dict-grid-meta 四 pageFn 3.3KB 样板收敛 host.__dictHelpers + html-page-generator 技能补「共享 escHtml/挂 host」两节。详见审查报告 §十。遗留：erp-voucher 两页 pageFn 样板、组件内直连 fetch ~53 处、auth.js。
>

## P2 — 规范清理与可选项

| 编号 | 事项 | 位置与证据 | 建议做法 | 验收标准 | 工作量 | 负责人 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| C-01 | 设计器属性面板 7 份 `esc` 一行流收敛 | `../../cmx-html-designer`：`models-props-{dataset,flexible-combination,meta,masterslave,columnmodel}.js`、`page-data-panel-models.js`；另 `designer-inspector/model-props-inspector.js` | 收一个本地 `utils/esc.js` 模块统一导出 | 目录内 esc 定义只剩 1 处；`npm test -w cmx-html-designer` + build 通过 | 0.5h | Qoder | ☑ 2026-09-01 |
| C-02 | `props-type-badge` 硬编码色值 → CSS 变量 | 上述 5 个 models-props 文件均为 `style="background:#XXX20;color:#XXX"` 内联硬编码；类已在 `designer-page-data-shell-style.js` 定义 | 色值下沉到 shell-style 的 `.props-type-badge[data-kind=...]`，用 `var(--sap*)` / `color-mix` 派生 | 切 UI5 暗色主题徽章可读；零硬编码 `#xxx`（符合 AGENTS §四.5） | 0.5h | Qoder | ☑ 2026-09-01 |
| C-03 | 裸色值分批治理 | 同行无 `var(--` 兜底的裸色值：assets ≈904、Portal ≈317、data-comp ≈180、Designer ≈165 | 先出分类报表（fallback 合规 / 图表色 / 真违规），只对"真违规"批处理；每批过 §四.5 自检 | 报表归档；真违规批清零；暗色主题 + Neo tone 切换抽测通过 | 2d+（分批） | Qoder | ☐（报表已出 2026-09-01，见下） |
| C-04 | （可选）`login.html` 共享 | `../../cmx-portal-manager` 与 `../../cmx-html-designer` 相似度 98.8%（421 行 ×2，仅 base 路径差异） | 抽共享模板 + 构建期注入 base；或接受现状但加"同步提醒"注释 | 两端登录页功能/视觉一致 | 0.5d | Qoder | ☑ 2026-09-01（注释方案） |
| C-05 | （可选）深拷贝收敛 | `clonePlain`(portal-definition-manager) / `cloneJson`(cmx-meta-model) / `clone`(flc-overlay) / `cloneArray`(cmx-field-clipboard) 语义相近 | 统一进 B-01 的 page-helpers 或 `cmx-data-comp/lib`；注意 `structuredClone` 原生可用场景直接替换 | 单测覆盖拷贝行为不回归 | 0.5d | Qoder | ☑ 2026-09-01 |

## 明确不动（审查中确认是合理差异，勿误伤）

- 9 个 `packages/cmx-data-comp/src/lib/*-neo-skin.js`：各组件皮肤实质不同（抽样 diff 43 增/42 删），仅"tone 三连行"是样板，收敛收益不抵耦合。
- `cmx-portal-manager` vs `cmx-html-designer` 的 `vite.config.js`（相似度 61%）：差异是真实构建差异。
- `scripts/run-vite.mjs`（30 行 ×3）：太小，抽共享不划算。
- 各主应用仓 `web/`、`data/` 下与 `cmx-container/assets/` 同名的目录：发布产物，设计使然（§四.7）。

## 依赖与建议顺序

```
A-01（缺陷，随时可修，独立）
A-02（独立，改两端登录链路，需手测未登录跳转）
B-01 → B-03 / B-04（先出口后清理；B-05 不依赖 B-01，可并行）
B-02（独立，但与 B-01 同改 barrel，建议同人）
C-01 / C-02（同目录，建议一次提交）
C-03（最大，依赖 B 系清完后再起，避免与存量清理互相干扰）
```
