# 技能库全面审查与优化报告

> 范围：根 `.agents/skills/`（9 个技能）+ `cmx-container/.agents/skills/`（原 15 个，去重后 14 个）。
> 方法：3 路并行盘点（目录级内容审查 / cmx-container 级内容审查 / 跨库重复与引用有效性核查），全部结论与代码实测核对后分三批实施（P0 错误修正 → P1 复用与结构 → P2 规范固化），每批经用户评审后推进。
> 日期：2026-08-25。

***

## 一、审查发现（按严重度）

### 🔴 严重（会产生错误指导，P0 已修）

| # | 问题 | 影响 |
|---|------|------|
| 1 | `axum-handler-generator`（2168 行）整份基于已拆除的 cmx-api 单体，全部路径失效（实际已拆 `crates/libs/cmx-apis/` 8 crate）；引用不存在的 `cmx-core/src/params.rs`、`cmx-core/src/dto/`；§6.4.4 "禁止 cmx-core 依赖 modql" 与 AGENTS.md 十四章冲突；request_body 口径三处分裂 | Rust 接口生成整体误导 |
| 2 | 根库 5 技能约 60 处引用已迁移的 `cmx-container/data/**` 旧路径（真源已迁 `assets/<svc>/**`） | 前端/菜单/元数据操作找不到文件 |
| 3 | `menu-generator` 的 `gen_menu_migration.mjs` 扫描不存在的旧目录，一跑即报错；脚本头注释"launcher 直读 JSON 即生效"与 SKILL.md 铁律矛盾 | 菜单 SQL 生成链路断裂 |
| 4 | `clippy-fix` 用 Windows `findstr`（Linux 不可用）、"修复后 cargo build" 违反全局规则 4 | 命令不可执行 / 违规 |
| 5 | `rust-arch-review` 的 reuse-catalog 大面积指向已消失 crate（cmx-api/cmx-portal/web-server）；"18 章"应为 19 章 | 架构审查 grep 落空 |
| 6 | `cmx-sql-execution` §2.0 依赖现状表引用三个已消失 crate | 双栈选型依据失实 |
| 7 | 组件数三个口径打架（84/92/实测 96） | 选型认知混乱 |
| 8 | 旧迁移文件引用失效（历史迁移已并入 platform/biz 两个 baseline；`cmx_code_rule` 实测在 **biz** 侧 baseline） | DDL 真源指引错误 |

### 🟡 一致性与可维护性（P1 已修）

- `plan-naming` 双库逐字节相同副本（双份维护）；html/native 各持 3 份逐字节相同 references（约 1100 行）+ Step 0/0.5 整段重复；`wasm-plugin-developer` §五整章（约 450 行）复制 `cmx-sql-execution`
- 超长正文堆细节：axum 2168 / cmx-sql-execution 808 / service-orchestration 712 / doc-crud-pages 547 / rust-comment-convention 631 / plugin-metadata-generator 559 / modql 432 / pg-table-generator 426 / doc-generator 411 / wasm 641 行
- frontmatter 三不统一（引号混用、Invoke when 中英夹杂、2 个全英文 description）
- pg-table-generator 示例用 `user`/`order` 保留字无法执行；审计字段 8 vs 9 口径不一（实测定谳 8 项：platform 48 表中 8 件套 37-46 表遵守、status 仅 26/48）
- modql 版本注记失实（实测本地 path 依赖 0.1.12、sea-query 1.0.0）；Sqlite* 三节教程在工程内不可用
- AGENTS.md 技能索引漏登 4 个根目录技能、"16 个/18 章/84 个"数字过时

### 🔵 建议（P2 已修 / 部分留档）

- 行号引用（`:374`、`#L8-L16` 等 129 处）与 `file:///` 绝对 URI——已全库清理为符号引用/相对路径
- display.mode 口径（6/7 之争）——实测定谳：options 7 个含空串，actions 属页面级
- meta-enricher "19 本 NoID 字典"——实测 60 本（`baseFieldSet=dictionaryCommonNoIDFields`）
- sql-guide §2.3 标题重复与内容错位、命名模板缺下划线；plugin-fn-doc §2.5 摘要句号违反自身规则；config-sync `env_template.toml` 笔误

## 二、三批改动明细

### P0 错误修正（根仓 21 文件 + container 7 文件）

data/→assets/ 路径批量修正（15 文件约 60 处，`doc-loader.js` 特例定位到 `assets/model/web/ui-native/`）；menu-generator 全面修复（脚本双路径兼容并干跑验证 4 文件/117 INSERT/0 冲突、`menu_seed.sql` 成为唯一产物、脚本归位 `scripts/`、`__pycache__` 清理 + .gitignore）；clippy-fix 命令与产物路径合规化；rust-arch-review 19 章 + reuse-catalog 按 cmx-apis 新架构重写（依赖现状 5+3+7 格局实测）；cmx-sql-execution §2.0 刷新；组件数统一 96（注明口径：34 显式 define + 63 thin、重叠 1）；flow-toolkit / mdm 迁移引用改指 platform/biz baseline；5 处真实死链修复（`cmx-grid-neo-skin.js` 迁移后旧链接、`ui5-runtime/client`、native 路由旧路径、`dct.rs/doc.rs` 改指 cmx-model 独立仓、`data/dict/` 旧目录）。

### P1 复用与结构（根仓 30 文件 + container 20 文件）

- **共享真源收敛**：component-catalog / field-edit-display-modes / page-style-guide 移入 `cmx-components-guide/references/`（约 1100 行三副本 → 一份）；plan-naming 删 container 副本（15→14）；wasm §五压为 WASM 差异速览 + cmx-sql-execution 拆 references 作后端共享真源；html/native Step 0/0.5 压为指针
- **超长技能拆分**（渐进式披露，全部 ≤400 行）：axum 2168→250（+2 refs，按 cmx-apis 新架构全面重写、import 写法逐一 grep 源码验证）、cmx-sql-execution 808→212、wasm 641→251、service-orchestration 712→312、doc-crud-pages 547→333、rust-comment-convention 631→279、plugin-metadata-generator 559→320、modql 432→189、pg-table-generator 426→197、doc-generator 411→244
- frontmatter 全库统一（11 技能改写，触发关键词全保留）；pg-table-generator 三方边界互链（含主键 varchar(64) vs BIGINT 口径声明）；plugin-metadata-generator 补"逻辑外键不生成 DDL FOREIGN KEY"；modql 删 Sqlite 教程

### P2 规范固化与索引

口径定谳（display.mode 7 值含空串 / NoID 60 本 / 审计 8 项）；行号与 file:/// 锚点全库清零；根 AGENTS.md 技能索引补 4 技能 + 数字修正（14 个/19 章/96 组件）+ 新增**技能编写规范**六条（frontmatter / 渐进式披露 / 引用原则 / 共享真源 / 产物文档 / 改完自检）；cmx-container AGENTS.md plan-naming 引用改指根真源；sql-guide / plugin-fn-doc / config-sync 笔误清理；本报告归档。

## 三、验证结果

| 检查项 | 结果 |
|--------|------|
| 仓库内路径引用（扩展名过滤 + 多基点解析） | 0 失效（余项为命名示例正误对照、按需产物模板） |
| 技能内 md 相对链接（294 条） | 0 失效（余项同类） |
| 原 4 对逐字节重复文件 | 已收敛为单一真源（diff 复验） |
| SKILL.md 行数红线 ≤400 | 全部达标（最高 388） |
| frontmatter 格式 | 无引号 / 无 Invoke when / 全中文触发句式 |
| `cmx-api/src` 旧路径残留 | 0 |
| 触发关键词 | 逐技能与原文比对无丢失 |
| 脚本健康 | gen_menu_migration.mjs 干跑通过；sync_menu_db.py 语法通过并增强参数解析 |

## 四、遗留事项与防腐建议（本次未动，供后续决策）

1. **`cmx-container/.trae/documents/` 120 个文件残留**、`cmx-container/documents/` 不存在——仓库级清理，超出技能库范围；rust-arch-review 的 `.trae/rules/project_rule.md` 引用当前有效，待 .trae 体系整体迁移时一并处理。
2. **`cmx-model` / `cmx-mega-sheet` 等独立仓的技能情况未审**——本次范围仅两个指定目录。
3. 防腐机制：新增/修改技能时执行根 AGENTS.md 新增的"技能编写规范"六条；引用数字（组件数/字典数/章数）在源结构变更时应同步实测更新——本次 96/60/8 三个数字建议在相关模块大改时复核。
