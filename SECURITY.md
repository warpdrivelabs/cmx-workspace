# 安全策略

## 支持的版本

| 版本 | 安全更新 |
|------|----------|
| 最新 `main` / 最新 release | ✅ |
| 旧版本 | ❌（请升级） |

## 报告漏洞

**请勿在公开 Issue 中披露安全漏洞。**

请通过以下方式私下报告：
- GitHub 的 **Private vulnerability reporting**（仓库 Settings → Security → 启用后可用）
- 或项目维护者提供的安全联系邮箱

报告请尽量包含：
- 受影响的组件 / 文件 / 版本
- 复现步骤或 PoC
- 影响评估（数据泄露 / 提权 / RCE 等）

## 处理流程与时间线
- 48 小时内确认收到
- 7 天内给出初步评估
- 修复后协调披露（默认 90 天，或按严重度调整）

## 本项目的安全须知（部署方必读）

- **配置凭据**：数据库 / Redis / 对象存储 / AI API Key 等**必须**通过环境变量或未纳入版本控制的本地配置提供（见 `config/config_template.toml`，复制为 `dev.toml` 填真实值）。仓库内**不含**任何真实凭据；`dev.toml` / `dev-local.toml` / `dev-vpn.toml` / `.env` 已被 `.gitignore` 忽略。
- **默认无鉴权的门户/设计器前端**：`portal` / `designer` 前端源自早期 Node 后端、原设计为内网/设计器场景，**生产部署务必置于鉴权网关之后**或开启内置鉴权。
- **多数据源路由**：`db_id` 路由到不同物理库，部署时确认连接串与网络隔离正确。
- **商业组件授权**：报表设计器等增强功能可能依赖商业授权组件（SpreadJS / Ignite UI）。开源产物本身不打包这些商业包；如需相关功能，请自备合法授权（详见 README 的「第三方与商业依赖」）。
