- 方案文档统一采用 `yyyyMMdd_模块名_中文标题.md` 命名规范，便于按时间排序与检索。
- 模型体系文档使用两位数字前缀（00~18）强制顺序阅读，并在 README 中提供小白/完整/重点问题三条阅读路径。
- 字段名、JSON key、API 名称在正文中以反引号代码风格标注，文件名/路径使用 file:// 可点击链接指向源码。
- 技术债文档在标题行标注实施状态（如“已实施 yyyyMMdd”），并通过表格索引维护议题清单与进度。
- 跨文档引用通过相对路径与 file:// 协议进行，避免硬编码绝对路径，保证在不同工作区下仍可跳转。