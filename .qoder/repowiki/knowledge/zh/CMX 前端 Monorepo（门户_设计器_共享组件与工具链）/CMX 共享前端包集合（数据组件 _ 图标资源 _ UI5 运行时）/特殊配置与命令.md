`cmx-data-comp`：`npm run test` / `test:watch` / `test:coverage`（Vitest），`npm run lint`（ESLint）；`cmx-icon-resource`：`node --test src/ui5/tabler-icon-bridge.test.js`；`cmx-ui5-runtime`：`npm run build` 与 `npm run dev` 通过 `scripts/run-vite.mjs` 调用 Vite，生产构建会注入 `__CMX_UI5_RUNTIME_ENTRY__` 全局变量指向带 hash 的 install chunk。