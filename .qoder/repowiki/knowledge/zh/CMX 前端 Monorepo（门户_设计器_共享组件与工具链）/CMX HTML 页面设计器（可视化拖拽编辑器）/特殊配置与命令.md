脚本统一经 `scripts/run-vite.mjs` 启动：`npm run dev` 启动开发服务器，`npm run build` 先构建依赖的 `cmx-ui5-runtime` 再打包设计器，`npm run preview` 预览产物；多环境通过 `.env` / `.env.production` 及 `--mode` 切换（如 `build:docker`）。Vite 代理 `/api`、`/rpc`、`/graphql`、`/sse`、`/ws` 等到 `CMX_VITE_BACKEND`（默认 `http://127.0.0.1:8080`）。