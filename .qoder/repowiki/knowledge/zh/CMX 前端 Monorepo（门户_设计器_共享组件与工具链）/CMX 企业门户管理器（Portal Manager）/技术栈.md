Vite 8 + Rollup/Rolldown 多入口构建（index.html / login.html）；UI5 Web Components 2.x（@ui5/webcomponents-*）作为基础控件；Lit 3 用于轻量组件；Fastify 5 作为遗留 mock 后端（scripts/run-vite.mjs 可切换）；CodeMirror 6 + Shiki 用于 JSON/代码编辑；tRPC + gRPC 通过代理访问 Rust 后端；Playwright 日志存放于 `.playwright-cli/`。