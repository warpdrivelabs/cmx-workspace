运行 `npm run dev` 通过 `scripts/run-vite.mjs` 启动 Vite 开发服务器；生产构建需先执行 `npm run build -w cmx-ui5-runtime` 再调用 `node scripts/run-vite.mjs build`；可通过 `.env.production` 设置 `CMX_APP_BASE=/portal/` 调整部署 base path；Mockoon 接口数据生成与校验通过 `npm run mockoon` 完成。