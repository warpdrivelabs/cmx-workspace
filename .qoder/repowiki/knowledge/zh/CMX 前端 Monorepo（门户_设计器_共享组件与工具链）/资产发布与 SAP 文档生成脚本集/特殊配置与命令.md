`publish-assets.sh <portal|model|mdm|flow|report|rules>` 需在工作区根目录下执行，依赖 `cmx-container/assets/<svc>` 存在且目标仓库目录已检出；`check-asset-ownership.py` 在 CI 中用于阻断违规提交（退出码 1）；三个文档生成脚本会向 `docs/.cache` 写入网络缓存，首次运行需可访问公网。