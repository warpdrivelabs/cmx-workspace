Python 3（标准库 `html.parser`、`pathlib`、`urllib.request`、`dataclasses`），Bash（`set -euo pipefail`、`comm`、`find -print0`）；外部数据源为 ERPExplorer (`www.erpexplorer.com`) 与 Data Panda (`datapanda.eu`) 的公开 SAP S/4HANA 表结构页面。